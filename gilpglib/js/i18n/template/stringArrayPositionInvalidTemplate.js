/**
 * @param {string | undefined} source
 * @param {number} i
 */
export function stringArrayPositionInvalidTemplate(source, i) {
 return `El valor de ${source}[${i}] no es string o señal de string.`
}