/**
 * @param {string | undefined} source
 * @param {number} i
 */
export function eventListenerTypePositionInvalidTemplate(source, i) {
 return `La primera posición de ${source}[${i}] no es string.`
}