/**
 * @param {string | undefined} source
 */
export function symbolePropertyNameInvalidTemplate(source) {
 return `Un símbolo usado como nombre para una propiedad de ${source
  } no tiene description tipo string no nula.`
}