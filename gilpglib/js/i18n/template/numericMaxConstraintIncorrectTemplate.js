/**
 * @param {string} nameForUser
 */
export function numericMaxConstraintIncorrectTemplate(nameForUser) {
 return `La restricción max para ${nameForUser
  } debe ser un texto que represente un número.`
}