/**
 * @param {string | undefined} source
 * @param {number} i
 */
export function eventListenerTypePositionEmptyTemplate(source, i) {
 return `La primera posición de ${source}[${i
  }] es symbol con description vacía.`
}