/**
 * @param {string | undefined} source
 * @param {number} i
 */
export function eventListenerListenerPositionInvalidTemplate(source, i) {
 return `La segunda posición de ${source}[${i}] no es una función.`
}